<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech News Central</title>
    <link rel="stylesheet" href="/public/styles/otherstyle.css">
  <link rel="stylesheet" href="/public/styles/style.css">

</head>

<body>
    <div class="wrapper">
        <header>
            <h1><a href="/">The Tech Blog</a></h1>
            <nav>
                {{#if loggedIn}}
                <a href="/">Home</a>
                <a href="/dashboard">Dashboard</a>
                <button id="logout" class="btn-no-style">Logout</button>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
                {{{ body }}} </main>
                
                <footer>
                    @Lando 2022
                </footer>
    </div>

    {{#if loggedIn}}
    <script src="/public/javascript/logout.js"></script>
    {{/if}}
</body>

</html>